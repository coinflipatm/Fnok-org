# fnok.org

One-page site for FNOK. Static HTML, deployed by Cloudflare Pages on push to `main`.

- `index.html` — the whole site
- `favicon.svg` — tab icon (redaction bar)
- `og.png` — share preview image (add 1200x630)
